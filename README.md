# st-lite-agent 前端插件

SillyTavern 前端插件(骨架)。

## 安装

1. 把本目录拷贝到 `<SillyTavern>/data/<你的用户名>/extensions/st-lite-agent/`
2. 重启酒馆,在「扩展」面板勾选启用 ST Lite Agent

## 用法

聊天输入框输入斜杠命令 `/lite-agent`,返回 pong 即加载正常。

## 后续

- 调用服务端插件暴露的接口
- 增加 Agent 面板 / 状态展示 UI

